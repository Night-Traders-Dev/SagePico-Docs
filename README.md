# SagePico Docs Site

Interactive documentation for SagePico — SageLang firmware for Feather RP2350.

Built with React + TypeScript + Vite + Tailwind CSS. Deployed via GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Output goes to `dist/`.
